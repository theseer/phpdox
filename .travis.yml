os:
- linux
- osx

language: php

php:
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - hhvm
  - hhvm-nightly

matrix:
  allow_failures:
    - php: hhvm
    - php: hhvm-nightly

before_script:
    - composer install --prefer-dist --dev
    - phpenv rehash

notifications:
  email: false
